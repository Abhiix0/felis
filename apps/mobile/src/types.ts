// Transitional: re-export shared types during monorepo migration
export type {
  Project,
  Task,
  Subtask,
  Recommendation,
  Priority,
  ProjectStatus,
  TaskStatus,
  FocusSessionStatus,
  IconType,
} from '@felis/types';
